#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 * Return: Always (Success)
 */

int main(void)
/* Write code here */

{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);
}
